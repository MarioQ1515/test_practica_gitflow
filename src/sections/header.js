export function renderHeader() {
  return `
    <header class="site-header">
      <p class="eyebrow">GitFlow Practice</p>
      <h1>Laboratorio de Pull Requests</h1>
      <p>Practica ramas, reviews, merges y releases con cambios pequenos y controlados.</p>
    </header>
  `;
}
